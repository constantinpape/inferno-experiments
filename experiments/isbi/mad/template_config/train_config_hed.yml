# The model arguments
model_name: HED
model_kwargs:
  in_channels: 1
  out_channels: 3
  initial_num_fmaps: 16
  fmap_growth: 4
  rescale_outputs: false

training_optimizer_kwargs:
  method: Adam
  lr: 0.0001
  weight_decay: 0.0005

devices:
  - 0
  - 1
