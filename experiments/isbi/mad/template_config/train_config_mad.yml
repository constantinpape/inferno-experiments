# The model arguments
model_name: MAD2D
model_kwargs:
  in_channels: 1
  out_channels: 3
  initial_num_fmaps: 16
  fmap_growth: 5

training_optimizer_kwargs:
  method: Adam
  lr: 0.0001
  weight_decay: 0.0005
  amsgrad: true

devices:
  - 0
  - 1
